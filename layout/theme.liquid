<!DOCTYPE html>
<html lang="fr">
<head>
    <link rel="stylesheet" href="{{ 'index-DxdBfIqj.css' | asset_url }}">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }} | {{ shop.name }}</title>
    <link rel="icon" href="{{ 'vite.svg' | asset_url }}" type="image/svg+xml">

    <!-- Styles -->
    <link rel="stylesheet" href="{{ 'index-DxdBfIqj.css' | asset_url }}">
    {{ content_for_header }}
</head>
<body class="min-h-screen bg-gray-50">
    <!-- En-tête -->
    {% section 'header' %}

    <!-- Bannière -->
    {% section 'banner' %}

    <!-- Contenu principal -->
    <main role="main" class="container mx-auto px-4">
        {{ content_for_layout }}
    </main>

    <!-- Pied de page -->
    {% section 'footer' %}

    {{ content_for_footer }}
</body>
</html>